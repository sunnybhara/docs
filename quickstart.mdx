---
title: "Quickstart"
sidebarTitle: "Quickstart"
description: "First API call in 3 minutes. Classify Arabic comments with a single request -- no credit card required."
---

# First API call in 3 minutes

Get up and running with the NAWA classification API in three steps.

<Steps>
  <Step title="Get your API key">
    Go to [trynawa.com/developers/keys](https://trynawa.com/developers/keys) to create your free API key. Sign in with Google or email -- no credit card required.

    <Note>
      Free keys (`nawa_test_sk_xxx`) require no credit card. Limited to 100 lifetime requests on paid endpoints and 14-day expiry. They use real AI models -- same accuracy as live keys.
    </Note>
  </Step>

  <Step title="Make your first classification">
    Send an Arabic comment to the `/v1/classify` endpoint:

    <CodeGroup>

    ```bash cURL
    curl -X POST https://api.trynawa.com/v1/classify \
      -H "Authorization: Bearer nawa_test_sk_xxx" \
      -H "Content-Type: application/json" \
      -d '{
        "text": "متى الجزء الثاني؟"
      }'
    ```

    ```typescript TypeScript
    import { Nawa } from '@nawalabs/sdk'

    const nawa = new Nawa({ apiKey: 'nawa_test_sk_xxx' })

    const { data, error } = await nawa.classify({
      text: 'متى الجزء الثاني؟'
    })

    if (error) {
      console.error(error.code, error.message)
    } else {
      console.log(data)
    }
    ```

    ```python Python
    from nawa import Nawa

    nawa = Nawa(api_key="nawa_test_sk_xxx")

    result = nawa.classify(
        text="متى الجزء الثاني؟"
    )

    if result.error:
        print(result.error.code, result.error.message)
    else:
        print(result.data)
    ```

    </CodeGroup>
  </Step>

  <Step title="Read the response">
    Every NAWA response follows a consistent envelope format:

    ```json
    {
      "result": {
        "id": "cls_nw_abc123def456",
        "object": "classification",
        "intent": ["question"],
        "sentiment": "neutral",
        "language": "ar",
        "dialect": "gulf",
        "dialect_confidence": 0.95,
        "requires_response": true,
        "priority": "medium",
        "suggested_reply": {
          "text": "إن شاء الله الجزء الثاني قريب!",
          "direction": "rtl"
        },
        "provider": "allam",
        "model": "allam-v2",
        "fallback_used": false,
        "cost_usd": 0.006,
        "credits_used": 6
      },
      "success": true,
      "errors": [],
      "request_id": "req_nw_abc123def456"
    }
    ```

    **Key response fields:**

    | Field | Type | Description |
    |-------|------|-------------|
    | `intent` | string[] | Detected intents: `question`, `complaint`, `praise`, `suggestion`, `spam`, `other` |
    | `sentiment` | string | One of: `positive`, `negative`, `neutral`, `mixed` |
    | `dialect` | string | One of: `gulf`, `egyptian`, `levantine`, `maghrebi`, `msa` |
    | `dialect_confidence` | number | Confidence score between 0 and 1 |
    | `requires_response` | boolean | Whether this comment needs a reply |
    | `priority` | string | One of: `low`, `medium`, `high` |
    | `suggested_reply` | object or null | AI-generated reply with `text` and `direction` |
    | `provider` | string | AI provider used: `allam`, `claude`, `gemini` |
    | `cost_usd` | number | Cost in USD (0 for free keys and cache hits) |
  </Step>
</Steps>

## What's next?

<CardGroup cols={2}>
  <Card title="Authentication" icon="key" href="/authentication">
    Learn about API keys, free vs live tiers, and key rotation.
  </Card>
  <Card title="API Reference" icon="code" href="/api-reference/classify">
    Explore all endpoints, parameters, and response schemas.
  </Card>
  <Card title="Dialect Detection" icon="language" href="/guides/arabic-dialect-detection">
    Deep dive into Gulf, Egyptian, Levantine, and MSA classification.
  </Card>
  <Card title="SDKs" icon="box" href="/sdks/typescript">
    Install the TypeScript or Python SDK for a better developer experience.
  </Card>
</CardGroup>
