---
title: "Classify and Reply"
sidebarTitle: "POST /v1/comments/reply"
description: "Classify a comment and generate a culturally-aware, dialect-matched reply in a single API call."
api: "POST https://api.trynawa.com/v1/comments/reply"
---

Classify a comment and generate an AI-powered reply in one call. The reply matches the commenter's dialect and cultural context.

<Note>
  Cost: **$0.008** per request (8 credits). This is the most feature-rich endpoint, combining classification and reply generation.
</Note>

## Request

### Body parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `text` | string | Yes | The comment text to classify and reply to. |
| `tone` | string | No | Reply tone: `friendly`, `professional`, `casual`, `formal`. Default: `professional`. |
| `max_length` | integer | No | Maximum reply length in characters (1--2000). Default: 500. |
| `context` | object | No | Additional context to improve reply relevance. |
| `context.platform` | string | No | Source platform (e.g. `youtube`, `instagram`). |
| `context.brand_voice` | string | No | Description of your brand voice or tone guidelines. |

### Query parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `provider` | string | No | Force a specific AI provider for A/B testing: `claude`, `gemini`, `allam`. |

### Example request

<CodeGroup>

```bash cURL
curl -X POST https://api.trynawa.com/v1/comments/reply \
  -H "Authorization: Bearer nawa_test_sk_xxx" \
  -H "Content-Type: application/json" \
  -d '{
    "text": "هذا المنتج سيء جداً ولا أنصح به",
    "tone": "professional",
    "max_length": 500,
    "context": {
      "platform": "youtube",
      "brand_voice": "Tech review channel focused on smartphones"
    }
  }'
```

```typescript TypeScript
import { Nawa } from '@nawalabs/sdk'

const nawa = new Nawa({ apiKey: process.env.NAWA_API_KEY })

const { data, error } = await nawa.comments.reply({
  text: 'هذا المنتج سيء جداً ولا أنصح به',
  tone: 'professional',
  maxLength: 500,
  context: {
    platform: 'youtube',
    brandVoice: 'Tech review channel focused on smartphones'
  }
})
```

```python Python
from nawa import Nawa

nawa = Nawa(api_key="your_api_key")

result = nawa.comments.reply(
    text="هذا المنتج سيء جداً ولا أنصح به",
    tone="professional",
    max_length=500,
    context={
        "platform": "youtube",
        "brand_voice": "Tech review channel focused on smartphones"
    }
)
```

</CodeGroup>

## Response

### Success response (200)

```json
{
  "success": true,
  "result": {
    "id": "rpl_nw_a1b2c3d4e5f6",
    "object": "comment_reply",
    "classification": {
      "intent": ["complaint"],
      "sentiment": "negative",
      "priority": "high",
      "requires_response": true
    },
    "reply": {
      "text": "نقدر ملاحظتك ونأسف على تجربتك. نحب نساعدك -- تقدر تتواصل معنا مباشرة عشان نحل المشكلة.",
      "direction": "rtl",
      "tone": "professional"
    },
    "provider": "allam",
    "model": "sdaia/allam-1-13b-instruct",
    "cost_usd": 0.008,
    "credits_used": 8
  },
  "errors": [],
  "request_id": "req_nw_rep789xyz012"
}
```

### Result fields

| Field | Type | Description |
|-------|------|-------------|
| `id` | string | Reply ID (`rpl_nw_xxx` format) |
| `object` | string | Always `comment_reply` |
| `classification.intent` | string[] | Detected intent(s) of the original comment |
| `classification.sentiment` | string | Detected sentiment |
| `classification.priority` | string | `high`, `medium`, or `low` |
| `classification.requires_response` | boolean | Whether the comment needs a reply |
| `reply.text` | string | The generated reply text |
| `reply.direction` | string | Text direction: `rtl` or `ltr` |
| `reply.tone` | string | The tone used for the reply |
| `provider` | string | AI provider used |
| `model` | string | Model version used |
| `cost_usd` | number | Cost in USD |
| `credits_used` | integer | Credits deducted |

### Error responses

| Status | Type | When |
|--------|------|------|
| 400 | `invalid_request_error` | Missing `text`. Invalid `tone` or `max_length`. |
| 401 | `authentication_error` | Invalid or missing API key |
| 402 | `insufficient_credits` | No credits remaining |
| 429 | `rate_limit_error` | Rate limit exceeded |
| 500 | `api_error` | Internal or provider error |
