---
title: "Usage"
sidebarTitle: "GET /v1/usage"
description: "Retrieve API usage statistics including request counts, credit consumption, and cache hit rates."
api: "GET https://api.trynawa.com/v1/usage"
---

Retrieve your API usage statistics including request counts, credit consumption, and cache hit rates. This endpoint is **free**.

## Request

### Query parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `from` | string | No | Start date (ISO 8601). Default: start of current month. |
| `to` | string | No | End date (ISO 8601). Default: now. |
| `group_by` | string | No | Group by: `endpoint` or `day`. Default: `endpoint`. |

### Example request

<CodeGroup>

```bash cURL
curl "https://api.trynawa.com/v1/usage?from=2026-01-01T00:00:00Z&group_by=endpoint" \
  -H "Authorization: Bearer nawa_test_sk_xxx"
```

```typescript TypeScript
import { Nawa } from '@nawalabs/sdk'

const nawa = new Nawa({ apiKey: process.env.NAWA_API_KEY })

const { data, error } = await nawa.usage({
  from: '2026-01-01T00:00:00Z',
  groupBy: 'endpoint'
})
```

```python Python
from nawa import Nawa

nawa = Nawa(api_key="your_api_key")

result = nawa.usage(
    from_date="2026-01-01T00:00:00Z",
    group_by="endpoint"
)
```

</CodeGroup>

## Response

### Success response (200)

```json
{
  "success": true,
  "result": {
    "usage": {
      "total_requests": 12450,
      "total_cost": 68.70,
      "cache_hits": 2860,
      "cache_hit_rate": 0.23,
      "period": {
        "from": "2026-01-01T00:00:00Z",
        "to": "2026-01-31T23:59:59Z"
      }
    },
    "by_endpoint": [
      {
        "endpoint": "/v1/classify",
        "requests": 8500,
        "cost": 51.00,
        "cache_hits": 1955
      },
      {
        "endpoint": "/v1/translate",
        "requests": 1200,
        "cost": 6.00,
        "cache_hits": 276
      },
      {
        "endpoint": "/v1/comments/reply",
        "requests": 1750,
        "cost": 14.00,
        "cache_hits": 0
      }
    ]
  },
  "errors": [],
  "request_id": "req_usg_abc123"
}
```

### Result fields

| Field | Type | Description |
|-------|------|-------------|
| `usage.total_requests` | integer | Total API requests in the period |
| `usage.total_cost` | number | Total cost in USD |
| `usage.cache_hits` | integer | Number of cache hits |
| `usage.cache_hit_rate` | number | Cache hit ratio (0-1) |
| `usage.period` | object | Date range applied |
| `by_endpoint` / `by_day` | array | Breakdown grouped by the `group_by` value |

<Tip>
  When grouped by `day`, each item uses a `date` key (e.g. `2026-01-15`) instead of `endpoint`.
</Tip>
