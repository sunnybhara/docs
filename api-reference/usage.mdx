---
title: "Usage"
sidebarTitle: "GET /v1/usage"
description: "Retrieve API usage statistics, credit consumption, and cache hit rates."
api: "GET https://api.trynawa.com/v1/usage"
---

Retrieve your API usage statistics including request counts, credit consumption, and cache hit rates. This endpoint is **free**.

## Request

### Query parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `from` | string | No | Start date (ISO 8601). Default: start of current month. |
| `to` | string | No | End date (ISO 8601). Default: now. |
| `group_by` | string | No | Group by: `endpoint` or `day`. Default: `endpoint`. |

### Example request

<CodeGroup>

```bash cURL
curl "https://api.trynawa.com/v1/usage?from=2025-01-01T00:00:00Z&group_by=endpoint" \
  -H "Authorization: Bearer nawa_live_sk_xxx"
```

```python Python
import requests

response = requests.get(
    "https://api.trynawa.com/v1/usage",
    headers={"Authorization": "Bearer nawa_live_sk_xxx"},
    params={
        "from": "2025-01-01T00:00:00Z",
        "group_by": "endpoint",
    },
)
result = response.json()
```

```typescript TypeScript
const params = new URLSearchParams({
  from: "2025-01-01T00:00:00Z",
  group_by: "endpoint",
});

const response = await fetch(
  `https://api.trynawa.com/v1/usage?${params}`,
  {
    headers: { Authorization: "Bearer nawa_live_sk_xxx" },
  }
);
const result = await response.json();
```

</CodeGroup>

## Response

### Success response (200)

```json
{
  "success": true,
  "result": {
    "usage": {
      "total_requests": 12450,
      "total_cost": 68.70,
      "cache_hits": 2231,
      "cache_hit_rate": 0.18,
      "period": {
        "from": "2025-01-01T00:00:00Z",
        "to": "2025-01-31T23:59:59Z"
      }
    },
    "by_endpoint": [
      {
        "endpoint": "/v1/classify",
        "requests": 8500,
        "cost": 51.00,
        "cache_hits": 1955
      },
      {
        "endpoint": "/v1/translate",
        "requests": 1200,
        "cost": 6.00,
        "cache_hits": 276
      },
      {
        "endpoint": "/v1/comments/reply",
        "requests": 1750,
        "cost": 14.00,
        "cache_hits": 0
      }
    ]
  },
  "errors": [],
  "request_id": "req_nw_a1b2c3d4e5f67890abcdef12"
}
```

### Result fields

| Field | Type | Description |
|-------|------|-------------|
| `usage.total_requests` | integer | Total API requests in the period |
| `usage.total_cost` | number | Total cost in USD |
| `usage.cache_hits` | integer | Number of cache hits |
| `usage.cache_hit_rate` | number | Cache hit rate (0--1) |
| `usage.period` | object | The `from` and `to` timestamps for the query |
| `by_endpoint` | array | Breakdown by endpoint (when `group_by=endpoint`) |
| `by_day` | array | Breakdown by day (when `group_by=day`) |

<Note>
  For more detailed analytics including latency, success rates, and provider distribution, use the [analytics endpoint](/api-reference/analytics).
</Note>

### Error responses

| Status | Type | When |
|--------|------|------|
| 401 | `authentication_error` | Invalid or missing API key |
| 429 | `rate_limit_error` | Rate limit exceeded |
| 500 | `api_error` | Internal error |
