---
title: "Submit feedback"
sidebarTitle: "POST /v1/feedback"
description: "Submit RLHF feedback on classification results to improve accuracy over time."
api: "POST https://api.trynawa.com/v1/feedback"
---

Submit reinforcement learning from human feedback (RLHF) on classification results. This endpoint is **free** for all tiers and helps improve NAWA's classification accuracy through quorum-based pattern confirmation.

<Tip>
  Every feedback submission helps improve accuracy for all NAWA users. Feedback on dialect detection edge cases is especially valuable.
</Tip>

## Request

### Headers

| Header | Required | Description |
|--------|----------|-------------|
| `Authorization` | Yes | `Bearer nawa_live_sk_xxx` or `Bearer nawa_test_sk_xxx` |
| `Content-Type` | Yes | `application/json` |

### Body parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `classification_id` | string | Yes | The `cls_nw_xxx` ID from a classify response. |
| `rating` | string | Yes | Accuracy rating: `correct`, `incorrect`, or `partial`. |
| `corrected_intent` | string[] | No | Corrected intent labels (e.g., `["question", "complaint"]`). |
| `corrected_sentiment` | string | No | Corrected sentiment: `positive`, `negative`, `neutral`, or `mixed`. |
| `comment` | string | No | Free-text explanation of why this correction is needed. |

### Example request

<CodeGroup>

```bash cURL
curl -X POST https://api.trynawa.com/v1/feedback \
  -H "Authorization: Bearer nawa_test_sk_xxx" \
  -H "Content-Type: application/json" \
  -d '{
    "classification_id": "cls_nw_a1b2c3d4e5f6",
    "rating": "incorrect",
    "corrected_intent": ["question"],
    "corrected_sentiment": "neutral",
    "comment": "This is a question, not a complaint"
  }'
```

```typescript TypeScript
import { Nawa } from '@nawalabs/sdk'

const nawa = new Nawa({ apiKey: process.env.NAWA_API_KEY })

const { data, error } = await nawa.feedback.submit({
  classificationId: 'cls_nw_a1b2c3d4e5f6',
  rating: 'incorrect',
  correctedIntent: ['question'],
  correctedSentiment: 'neutral',
  comment: 'This is a question, not a complaint'
})
```

```python Python
from nawa import Nawa

nawa = Nawa(api_key="your_api_key")

result = nawa.feedback.submit(
    classification_id="cls_nw_a1b2c3d4e5f6",
    rating="incorrect",
    corrected_intent=["question"],
    corrected_sentiment="neutral",
    comment="This is a question, not a complaint"
)
```

</CodeGroup>

## Response

### Success response (200)

```json
{
  "result": {
    "id": "fb_nw_a1b2c3d4e5f6",
    "object": "feedback",
    "classification_id": "cls_nw_a1b2c3d4e5f6",
    "rating": "incorrect",
    "acknowledged": true
  },
  "success": true,
  "errors": [],
  "request_id": "req_nw_d4e5f6a7b8c9d0e1f2a3b4c5"
}
```

### Result fields

| Field | Type | Description |
|-------|------|-------------|
| `id` | string | Feedback ID (`fb_nw_xxx` format) |
| `object` | string | Always `"feedback"` |
| `classification_id` | string | The classification this feedback refers to |
| `rating` | string | The submitted rating |
| `acknowledged` | boolean | Always `true` on success |

### Error responses

| Status | Type | When |
|--------|------|------|
| 400 | `invalid_request_error` | Missing `classification_id` or `rating`, invalid values |
| 401 | `authentication_error` | Invalid or missing API key |
| 429 | `rate_limit_error` | Rate limit exceeded |
| 500 | `api_error` | Internal error |
