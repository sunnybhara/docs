---
title: "Analytics"
sidebarTitle: "GET /v1/analytics"
description: "Aggregated analytics on request volume, costs, cache performance, and provider distribution."
api: "GET https://api.trynawa.com/v1/analytics"
---

Retrieve aggregated analytics across your API usage. This endpoint is **free**.

## Request

### Query parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `from` | string | No | Start date (ISO 8601) |
| `to` | string | No | End date (ISO 8601) |
| `group_by` | string | No | Group results by: `endpoint`, `day`, `provider`. Default: `endpoint`. |

### Example request

<CodeGroup>

```bash cURL
curl "https://api.trynawa.com/v1/analytics?from=2025-01-01T00:00:00Z&to=2025-01-31T23:59:59Z&group_by=endpoint" \
  -H "Authorization: Bearer nawa_live_sk_xxx"
```

```typescript TypeScript
import { Nawa } from '@nawalabs/sdk'

const nawa = new Nawa({ apiKey: process.env.NAWA_API_KEY })

const { data, error } = await nawa.analytics({
  from: '2025-01-01T00:00:00Z',
  to: '2025-01-31T23:59:59Z',
  groupBy: 'endpoint'
})
```

```python Python
from nawa import Nawa

nawa = Nawa(api_key="your_api_key")

result = nawa.analytics(
    from_date="2025-01-01T00:00:00Z",
    to_date="2025-01-31T23:59:59Z",
    group_by="endpoint"
)
```

</CodeGroup>

## Response

### Success response (200)

```json
{
  "success": true,
  "result": {
    "analytics": {
      "total_requests": 4521,
      "total_cost": 28.536,
      "cache_hits": 1042,
      "cache_hit_rate": 0.23,
      "avg_latency_ms": 820,
      "success_rate": 0.98,
      "error_count": 90,
      "endpoint_distribution": {
        "/v1/classify": 3200,
        "/v1/rubric-classify": 820,
        "/v1/comments/reply": 501
      },
      "provider_distribution": {
        "claude": 2100,
        "allam": 1890,
        "gemini": 531
      },
      "period": {
        "from": "2025-01-01T00:00:00Z",
        "to": "2025-01-31T23:59:59Z"
      }
    },
    "by_endpoint": [
      {
        "endpoint": "/v1/classify",
        "requests": 3200,
        "cost": 19.2,
        "avg_latency_ms": 780
      },
      {
        "endpoint": "/v1/rubric-classify",
        "requests": 820,
        "cost": 2.46,
        "avg_latency_ms": 650
      },
      {
        "endpoint": "/v1/comments/reply",
        "requests": 501,
        "cost": 4.008,
        "avg_latency_ms": 1200
      }
    ]
  },
  "errors": [],
  "request_id": "req_nw_ana_abc123def456ghij78"
}
```

### Result fields

| Field | Type | Description |
|-------|------|-------------|
| `analytics.total_requests` | integer | Total API requests in the period |
| `analytics.total_cost` | number | Total cost in USD |
| `analytics.cache_hits` | integer | Number of cache hits |
| `analytics.cache_hit_rate` | number | Cache hit ratio (0-1) |
| `analytics.avg_latency_ms` | integer | Average response time in milliseconds |
| `analytics.success_rate` | number | Success ratio (0-1) |
| `analytics.error_count` | integer | Total error responses |
| `analytics.endpoint_distribution` | object | Request counts by endpoint |
| `analytics.provider_distribution` | object | Request counts by AI provider |
| `analytics.period` | object | The query time range |
| `by_endpoint` / `by_day` / `by_provider` | array | Breakdown grouped by the `group_by` parameter |
