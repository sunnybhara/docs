---
title: "Classify Comment"
sidebarTitle: "POST /v1/classify"
description: "Classify an Arabic or English comment by intent, sentiment, dialect, and toxicity in a single API call."
api: "POST https://api.trynawa.com/v1/classify"
---

Classify any comment with a single request. Returns intent, sentiment, dialect, and toxicity analysis. Arabic comments are routed to HUMAIN's ALLaM model for dialect detection. English comments are routed to Claude for high-accuracy classification.

## Request

<Note>
  Cost: **$0.006** per request. Semantic cache hits are free (`X-NAWA-Cache: HIT`).
</Note>

### Headers

| Header | Required | Description |
|--------|----------|-------------|
| `Authorization` | Yes | `Bearer nawa_live_sk_xxx` or `Bearer nawa_test_sk_xxx` |
| `Content-Type` | Yes | `application/json` |

### Query parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `provider` | string | No | Override the default provider routing. Must be one of: `claude`, `gemini`, `allam`. Useful for A/B testing across providers. |

### Body parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `text` | string | Yes | The comment text to classify. Max 5,000 characters. |
| `platform` | string | No | Source platform: `youtube`, `instagram`, `twitter`, `facebook`. Improves accuracy with platform-specific context. |
| `channel_id` | string | No | Your channel or account identifier for analytics grouping. |
| `metadata` | object | No | Arbitrary key-value metadata to attach to the classification. |

### Example request

<CodeGroup>

```bash cURL
curl -X POST https://api.trynawa.com/v1/classify \
  -H "Authorization: Bearer nawa_test_sk_xxx" \
  -H "Content-Type: application/json" \
  -d '{
    "text": "متى الجزء الثاني؟",
    "platform": "youtube",
    "channel_id": "ch_123"
  }'
```

```typescript TypeScript
import { Nawa } from '@nawalabs/sdk'

const nawa = new Nawa({ apiKey: process.env.NAWA_API_KEY })

const { data, error } = await nawa.classify({
  text: 'متى الجزء الثاني؟',
  platform: 'youtube',
  channelId: 'ch_123'
})
```

```python Python
from nawa import Nawa

nawa = Nawa(api_key="your_api_key")

result = nawa.classify(
    text="متى الجزء الثاني؟",
    platform="youtube",
    channel_id="ch_123"
)
```

</CodeGroup>

## Response

### Success response (200)

```json
{
  "success": true,
  "result": {
    "id": "cls_nw_abc123def456",
    "object": "classification",
    "intent": "question",
    "sentiment": "neutral",
    "language": "ar",
    "dialect": "gulf",
    "dialect_confidence": 0.95,
    "requires_response": true,
    "priority": "medium",
    "suggested_reply": {
      "text": "The model's suggested response text",
      "direction": "rtl"
    },
    "provider": "allam",
    "model": "nagl-v1",
    "fallback_used": false,
    "tokens_used": null,
    "cost_usd": 0.006,
    "credits_used": 6
  },
  "errors": [],
  "request_id": "req_nw_abc123def456"
}
```

### Response headers

| Header | Description |
|--------|-------------|
| `X-Request-Id` | Unique request identifier for debugging |
| `X-RateLimit-Limit` | Rate limit ceiling for current window |
| `X-RateLimit-Remaining` | Requests remaining in current window |
| `X-RateLimit-Reset` | Window reset time (RFC 3339) |
| `X-NAWA-Cache` | `HIT` if served from semantic cache (no cost), `MISS` otherwise |
| `X-NAWA-Provider` | The AI provider used for this request (e.g., `claude`, `allam`, `gemini`) |
| `X-NAWA-Latency` | Server-side processing time in milliseconds |
| `X-NAWA-Calibration-Version` | Version identifier for the calibration thresholds applied |

### Result fields

| Field | Type | Description |
|-------|------|-------------|
| `id` | string | Unique classification identifier (prefixed with `cls_nw_`) |
| `object` | string | Always `"classification"` |
| `intent` | string | `question`, `complaint`, `praise`, `suggestion`, `spam`, `other` |
| `sentiment` | string | `positive`, `negative`, `neutral`, `mixed` |
| `language` | string | Detected language code (`ar`, `en`) |
| `dialect` | string \| null | `gulf`, `egyptian`, `levantine`, `msa`. Returns `null` for non-Arabic text. |
| `dialect_confidence` | number \| null | Confidence score (0-1). Returns `null` for non-Arabic text. |
| `requires_response` | boolean | Whether the comment warrants a reply |
| `priority` | string | Priority level for the comment |
| `suggested_reply` | object | Suggested reply object (see below) |
| `suggested_reply.text` | string | Model-generated reply suggestion. Empty string when the `arabizi-rubric` model is used, since that model uses pattern matching rather than LLM reasoning. |
| `suggested_reply.direction` | string | Text direction: `rtl` for Arabic, `ltr` for English |
| `provider` | string | AI provider used: `claude`, `gemini`, or `allam` |
| `model` | string | Model version used for classification |
| `fallback_used` | boolean | Whether a fallback provider was used |
| `tokens_used` | null | Reserved for future use |
| `cost_usd` | number | Cost of this request in USD (`0.006`) |
| `credits_used` | number | Credits deducted for this request (`6`) |

### Error responses

| Status | Type | When |
|--------|------|------|
| 400 | `invalid_request_error` | Missing `text`, invalid `platform`, text too long |
| 401 | `authentication_error` | Invalid or missing API key |
| 402 | `insufficient_credits` | No credits remaining |
| 429 | `rate_limit_error` | Rate limit exceeded |
| 500 | `api_error` | Internal or provider error |

### More examples

<AccordionGroup>
  <Accordion title="English - Praise">
    ```bash
    curl -X POST https://api.trynawa.com/v1/classify \
      -H "Authorization: Bearer nawa_test_sk_xxx" \
      -H "Content-Type: application/json" \
      -d '{"text": "This is hands down the best review I have seen on this phone. Subscribed!", "platform": "youtube"}'
    ```

    Response:
    ```json
    {
      "success": true,
      "result": {
        "id": "cls_nw_en_praise_001",
        "object": "classification",
        "intent": "praise",
        "sentiment": "positive",
        "language": "en",
        "dialect": null,
        "dialect_confidence": null,
        "requires_response": false,
        "priority": "low",
        "suggested_reply": {
          "text": "Thank you for the kind words!",
          "direction": "ltr"
        },
        "provider": "claude",
        "model": "claude-v1",
        "fallback_used": false,
        "tokens_used": null,
        "cost_usd": 0.006,
        "credits_used": 6
      },
      "errors": [],
      "request_id": "req_nw_en_praise_001"
    }
    ```
  </Accordion>

  <Accordion title="English - Complaint">
    ```bash
    curl -X POST https://api.trynawa.com/v1/classify \
      -H "Authorization: Bearer nawa_test_sk_xxx" \
      -H "Content-Type: application/json" \
      -d '{"text": "The audio quality is terrible in this one. Can barely hear anything after the 5 minute mark.", "platform": "youtube"}'
    ```

    Response:
    ```json
    {
      "success": true,
      "result": {
        "id": "cls_nw_en_compl_001",
        "object": "classification",
        "intent": "complaint",
        "sentiment": "negative",
        "language": "en",
        "dialect": null,
        "dialect_confidence": null,
        "requires_response": true,
        "priority": "high",
        "suggested_reply": {
          "text": "We appreciate the feedback and will look into improving audio quality.",
          "direction": "ltr"
        },
        "provider": "claude",
        "model": "claude-v1",
        "fallback_used": false,
        "tokens_used": null,
        "cost_usd": 0.006,
        "credits_used": 6
      },
      "errors": [],
      "request_id": "req_nw_en_compl_001"
    }
    ```
  </Accordion>

  <Accordion title="English - Question">
    ```bash
    curl -X POST https://api.trynawa.com/v1/classify \
      -H "Authorization: Bearer nawa_test_sk_xxx" \
      -H "Content-Type: application/json" \
      -d '{"text": "What camera and lens setup are you using for these shots?", "platform": "youtube"}'
    ```

    Response:
    ```json
    {
      "success": true,
      "result": {
        "id": "cls_nw_en_quest_001",
        "object": "classification",
        "intent": "question",
        "sentiment": "neutral",
        "language": "en",
        "dialect": null,
        "dialect_confidence": null,
        "requires_response": true,
        "priority": "medium",
        "suggested_reply": {
          "text": "Great question! We use a Sony A7IV with a 24-70mm f/2.8 lens.",
          "direction": "ltr"
        },
        "provider": "claude",
        "model": "claude-v1",
        "fallback_used": false,
        "tokens_used": null,
        "cost_usd": 0.006,
        "credits_used": 6
      },
      "errors": [],
      "request_id": "req_nw_en_quest_001"
    }
    ```
  </Accordion>

  <Accordion title="Gulf Arabic - Praise">
    ```bash
    curl -X POST https://api.trynawa.com/v1/classify \
      -H "Authorization: Bearer nawa_test_sk_xxx" \
      -H "Content-Type: application/json" \
      -d '{"text": "ما شاء الله عليك، محتوى رهيب!", "platform": "youtube"}'
    ```

    Response:
    ```json
    {
      "success": true,
      "result": {
        "id": "cls_nw_ar_praise_001",
        "object": "classification",
        "intent": "praise",
        "sentiment": "positive",
        "language": "ar",
        "dialect": "gulf",
        "dialect_confidence": 0.93,
        "requires_response": false,
        "priority": "low",
        "suggested_reply": {
          "text": "شكراً لك على كلماتك الطيبة!",
          "direction": "rtl"
        },
        "provider": "allam",
        "model": "nagl-v1",
        "fallback_used": false,
        "tokens_used": null,
        "cost_usd": 0.006,
        "credits_used": 6
      },
      "errors": [],
      "request_id": "req_nw_ar_praise_001"
    }
    ```
  </Accordion>

  <Accordion title="Egyptian Arabic - Complaint">
    ```bash
    curl -X POST https://api.trynawa.com/v1/classify \
      -H "Authorization: Bearer nawa_test_sk_xxx" \
      -H "Content-Type: application/json" \
      -d '{"text": "الصوت وحش أوي في الفيديو ده", "platform": "youtube"}'
    ```

    Response:
    ```json
    {
      "success": true,
      "result": {
        "id": "cls_nw_ar_compl_001",
        "object": "classification",
        "intent": "complaint",
        "sentiment": "negative",
        "language": "ar",
        "dialect": "egyptian",
        "dialect_confidence": 0.97,
        "requires_response": true,
        "priority": "high",
        "suggested_reply": {
          "text": "نعتذر عن جودة الصوت وسنعمل على تحسينها.",
          "direction": "rtl"
        },
        "provider": "allam",
        "model": "nagl-v1",
        "fallback_used": false,
        "tokens_used": null,
        "cost_usd": 0.006,
        "credits_used": 6
      },
      "errors": [],
      "request_id": "req_nw_ar_compl_001"
    }
    ```
  </Accordion>

  <Accordion title="Levantine Arabic - Suggestion">
    ```bash
    curl -X POST https://api.trynawa.com/v1/classify \
      -H "Authorization: Bearer nawa_test_sk_xxx" \
      -H "Content-Type: application/json" \
      -d '{"text": "لو سمحت حاول تحكي عن المطاعم بلبنان", "platform": "instagram"}'
    ```

    Response:
    ```json
    {
      "success": true,
      "result": {
        "id": "cls_nw_ar_sugst_001",
        "object": "classification",
        "intent": "suggestion",
        "sentiment": "neutral",
        "language": "ar",
        "dialect": "levantine",
        "dialect_confidence": 0.96,
        "requires_response": true,
        "priority": "medium",
        "suggested_reply": {
          "text": "شكراً على الاقتراح! رح نحاول نغطي مطاعم لبنان قريباً.",
          "direction": "rtl"
        },
        "provider": "allam",
        "model": "nagl-v1",
        "fallback_used": false,
        "tokens_used": null,
        "cost_usd": 0.006,
        "credits_used": 6
      },
      "errors": [],
      "request_id": "req_nw_ar_sugst_001"
    }
    ```
  </Accordion>

  <Accordion title="Arabizi input (pattern-matched)">
    When NAWA detects Arabizi (Arabic written in Latin script), the `arabizi-rubric` model handles
    classification via pattern matching. In this case, `suggested_reply.text` returns an empty string
    because the model does not use LLM reasoning.

    ```bash
    curl -X POST https://api.trynawa.com/v1/classify \
      -H "Authorization: Bearer nawa_test_sk_xxx" \
      -H "Content-Type: application/json" \
      -d '{"text": "7abibi shu hal video el 7elo", "platform": "youtube"}'
    ```

    Response:
    ```json
    {
      "success": true,
      "result": {
        "id": "cls_nw_arb_001",
        "object": "classification",
        "intent": "praise",
        "sentiment": "positive",
        "language": "ar",
        "dialect": "gulf",
        "dialect_confidence": 0.88,
        "requires_response": false,
        "priority": "low",
        "suggested_reply": {
          "text": "",
          "direction": "rtl"
        },
        "provider": "allam",
        "model": "arabizi-rubric",
        "fallback_used": false,
        "tokens_used": null,
        "cost_usd": 0.006,
        "credits_used": 6
      },
      "errors": [],
      "request_id": "req_nw_arb_001"
    }
    ```
  </Accordion>
</AccordionGroup>
