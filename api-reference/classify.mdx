---
title: "Classify text"
sidebarTitle: "POST /v1/classify"
description: "Classify Arabic or English text by intent, sentiment, dialect, and priority in a single API call."
api: "POST https://api.trynawa.com/v1/classify"
---

Classify Arabic or English text with a single request. Returns intent, sentiment, dialect, priority, and a suggested reply direction. Supports semantic caching and provider selection.

## Request

<Note>
  Cost: **$0.006** per request (6 credits). Semantic cache hits are free (`X-NAWA-Cache: HIT`).
</Note>

### Headers

| Header | Required | Description |
|--------|----------|-------------|
| `Authorization` | Yes | `Bearer nawa_live_sk_xxx` or `Bearer nawa_test_sk_xxx` |
| `Content-Type` | Yes | `application/json` |

### Query parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `provider` | string | No | Force a specific AI provider for A/B testing: `claude`, `gemini`, `allam`. |

### Body parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `text` | string | Yes | The text to classify. Min 1 character. |
| `context` | object | No | Optional context object to improve classification accuracy. |

### Example request

<CodeGroup>

```bash cURL
curl -X POST https://api.trynawa.com/v1/classify \
  -H "Authorization: Bearer nawa_live_sk_xxx" \
  -H "Content-Type: application/json" \
  -d '{
    "text": "متى الجزء الثاني؟"
  }'
```

```typescript TypeScript
import { Nawa } from '@nawalabs/sdk'

const nawa = new Nawa({ apiKey: process.env.NAWA_API_KEY })

const { data, error } = await nawa.classify({
  text: 'متى الجزء الثاني؟'
})
```

```python Python
from nawa import Nawa

nawa = Nawa(api_key="your_api_key")

result = nawa.classify(
    text="متى الجزء الثاني؟"
)
```

</CodeGroup>

## Response

### Success response (200)

```json
{
  "success": true,
  "result": {
    "id": "cls_nw_a1b2c3d4e5f6",
    "object": "classification",
    "intent": ["question"],
    "sentiment": "neutral",
    "language": "ar",
    "dialect": "gulf",
    "dialect_confidence": 0.92,
    "requires_response": true,
    "priority": "medium",
    "suggested_reply": {
      "text": "سؤال من المستخدم عن الجزء التالي من المحتوى",
      "direction": "rtl"
    },
    "provider": "allam",
    "model": "sdaia/allam-1-13b-instruct",
    "fallback_used": false,
    "tokens_used": null,
    "cost_usd": 0.006,
    "credits_used": 6
  },
  "errors": [],
  "request_id": "req_nw_a1b2c3d4e5f67890abcdef12"
}
```

### Response headers

| Header | Description |
|--------|-------------|
| `X-Request-Id` | Unique request identifier (`req_nw_xxx` format) |
| `X-NAWA-Provider` | AI provider used: `claude`, `gemini`, or `allam` |
| `X-NAWA-Cache` | `HIT` if served from semantic cache (no cost), `MISS` otherwise |
| `X-NAWA-Latency` | Processing latency in milliseconds |
| `X-NAWA-Calibration-Version` | Calibration version applied (if any) |
| `X-RateLimit-Limit` | Rate limit ceiling for current window |
| `X-RateLimit-Remaining` | Requests remaining in current window |
| `X-RateLimit-Reset` | Window reset time (RFC 3339) |

### Result fields

| Field | Type | Description |
|-------|------|-------------|
| `id` | string | Classification ID (`cls_nw_xxx` format) |
| `object` | string | Always `classification` |
| `intent` | string[] | Detected intents: `praise`, `question`, `complaint`, `suggestion`, `spam`, `other` |
| `sentiment` | string | `positive`, `negative`, `neutral`, `mixed` |
| `language` | string | Detected language: `ar`, `en`, `mixed` |
| `dialect` | string or null | Arabic dialect: `gulf`, `egyptian`, `levantine`, `maghrebi`, `msa`. Null if not Arabic. |
| `dialect_confidence` | number or null | Confidence score (0-1). Null if not Arabic. |
| `requires_response` | boolean | Whether NAWA recommends responding to this comment |
| `priority` | string | `high`, `medium`, `low` |
| `suggested_reply` | object or null | Suggested reply with `text` and `direction` (`ltr` or `rtl`) |
| `provider` | string | AI provider used: `claude`, `gemini`, `allam` |
| `model` | string | Specific model version used |
| `fallback_used` | boolean | Whether a fallback provider was used |
| `tokens_used` | integer or null | Token count (when available) |
| `cost_usd` | number | Cost in USD |
| `credits_used` | integer | Credits deducted from your balance |

### Error responses

| Status | Type | When |
|--------|------|------|
| 400 | `invalid_request_error` | Missing `text` or empty body |
| 401 | `authentication_error` | Invalid or missing API key |
| 402 | `insufficient_credits` | No credits remaining |
| 429 | `rate_limit_error` | Rate limit exceeded |
| 500 | `api_error` | Internal or provider error |

### More Arabic examples

<AccordionGroup>
  <Accordion title="Gulf Arabic - Praise">
    ```bash
    curl -X POST https://api.trynawa.com/v1/classify \
      -H "Authorization: Bearer nawa_live_sk_xxx" \
      -H "Content-Type: application/json" \
      -d '{"text": "ماشاء الله الفيديو حلو مرة"}'
    ```

    Response:
    ```json
    {
      "success": true,
      "result": {
        "id": "cls_nw_f7g8h9i0j1k2",
        "object": "classification",
        "intent": ["praise"],
        "sentiment": "positive",
        "language": "ar",
        "dialect": "gulf",
        "dialect_confidence": 0.92,
        "requires_response": false,
        "priority": "medium",
        "suggested_reply": {
          "text": "تعليق إيجابي يعبر عن إعجاب المستخدم بالمحتوى",
          "direction": "rtl"
        },
        "provider": "allam",
        "model": "sdaia/allam-1-13b-instruct",
        "fallback_used": false,
        "tokens_used": null,
        "cost_usd": 0.006,
        "credits_used": 6
      },
      "errors": [],
      "request_id": "req_nw_ghi789jkl012mnopqrstuv"
    }
    ```
  </Accordion>

  <Accordion title="Egyptian Arabic - Complaint">
    ```bash
    curl -X POST https://api.trynawa.com/v1/classify \
      -H "Authorization: Bearer nawa_live_sk_xxx" \
      -H "Content-Type: application/json" \
      -d '{"text": "الصوت وحش أوي في الفيديو ده"}'
    ```

    Response:
    ```json
    {
      "success": true,
      "result": {
        "id": "cls_nw_m3n4o5p6q7r8",
        "object": "classification",
        "intent": ["complaint"],
        "sentiment": "negative",
        "language": "ar",
        "dialect": "egyptian",
        "dialect_confidence": 0.97,
        "requires_response": true,
        "priority": "high",
        "suggested_reply": null,
        "provider": "allam",
        "model": "sdaia/allam-1-13b-instruct",
        "fallback_used": false,
        "tokens_used": null,
        "cost_usd": 0.006,
        "credits_used": 6
      },
      "errors": [],
      "request_id": "req_nw_mno345pqr678stuvwxyz12"
    }
    ```
  </Accordion>

  <Accordion title="Levantine Arabic - Suggestion">
    ```bash
    curl -X POST https://api.trynawa.com/v1/classify \
      -H "Authorization: Bearer nawa_live_sk_xxx" \
      -H "Content-Type: application/json" \
      -d '{"text": "لو سمحت حاول تحكي عن المطاعم بلبنان"}'
    ```

    Response:
    ```json
    {
      "success": true,
      "result": {
        "id": "cls_nw_s9t0u1v2w3x4",
        "object": "classification",
        "intent": ["suggestion"],
        "sentiment": "neutral",
        "language": "ar",
        "dialect": "levantine",
        "dialect_confidence": 0.96,
        "requires_response": false,
        "priority": "low",
        "suggested_reply": null,
        "provider": "allam",
        "model": "sdaia/allam-1-13b-instruct",
        "fallback_used": false,
        "tokens_used": null,
        "cost_usd": 0.006,
        "credits_used": 6
      },
      "errors": [],
      "request_id": "req_nw_stu901vwx234yzabcdef56"
    }
    ```
  </Accordion>

  <Accordion title="English comment">
    ```bash
    curl -X POST https://api.trynawa.com/v1/classify \
      -H "Authorization: Bearer nawa_live_sk_xxx" \
      -H "Content-Type: application/json" \
      -d '{"text": "Great video, love the content!"}'
    ```

    Response:
    ```json
    {
      "success": true,
      "result": {
        "id": "cls_nw_y5z6a7b8c9d0",
        "object": "classification",
        "intent": ["praise"],
        "sentiment": "positive",
        "language": "en",
        "dialect": null,
        "dialect_confidence": null,
        "requires_response": false,
        "priority": "low",
        "suggested_reply": {
          "text": "Positive comment expressing appreciation for the content",
          "direction": "ltr"
        },
        "provider": "claude",
        "model": "claude-haiku-4-5-20251001",
        "fallback_used": false,
        "tokens_used": null,
        "cost_usd": 0.006,
        "credits_used": 6
      },
      "errors": [],
      "request_id": "req_nw_yza567bcd890efghijkl12"
    }
    ```
  </Accordion>
</AccordionGroup>
