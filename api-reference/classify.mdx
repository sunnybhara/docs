---
title: "Classify text"
sidebarTitle: "POST /v1/classify"
description: "Classify Arabic or English text by intent, sentiment, dialect, and priority in a single API call."
api: "POST https://api.trynawa.com/v1/classify"
---

Classify Arabic or English text. Returns intent, sentiment, dialect, priority, and a suggested reply direction. Supports semantic caching and provider selection for A/B testing.

<Note>
  Cost: **$0.006** per request (6 credits). Semantic cache hits are free (`X-NAWA-Cache: HIT`).
</Note>

## Request

### Headers

| Header | Required | Description |
|--------|----------|-------------|
| `Authorization` | Yes | `Bearer nawa_live_sk_xxx` or `Bearer nawa_test_sk_xxx` |
| `Content-Type` | Yes | `application/json` |

### Query parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `provider` | string | No | Force a specific AI provider: `claude`, `gemini`, `allam`. Useful for A/B testing. |

### Body parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `text` | string | Yes | The text to classify. Max 5,000 characters. |
| `context` | object | No | Optional context object to improve classification accuracy. |

### Example request

<CodeGroup>

```bash cURL
curl -X POST https://api.trynawa.com/v1/classify \
  -H "Authorization: Bearer nawa_test_sk_xxx" \
  -H "Content-Type: application/json" \
  -d '{
    "text": "متى الجزء الثاني؟"
  }'
```

```typescript TypeScript
import { Nawa } from '@nawalabs/sdk'

const nawa = new Nawa({ apiKey: process.env.NAWA_API_KEY })

const result = await nawa.classify({
  text: 'متى الجزء الثاني؟'
})

console.log(result.intent)      // ["question"]
console.log(result.dialect)     // "gulf"
console.log(result.priority)    // "medium"
```

```python Python
from nawa import Nawa

nawa = Nawa(api_key="your_api_key")

result = nawa.classify(
    text="متى الجزء الثاني؟"
)

print(result.intent)       # ["question"]
print(result.dialect)      # "gulf"
print(result.priority)     # "medium"
```

</CodeGroup>

## Response

### Success response (200)

```json
{
  "success": true,
  "result": {
    "id": "cls_nw_a1b2c3d4e5f6",
    "object": "classification",
    "intent": ["question"],
    "sentiment": "neutral",
    "language": "ar",
    "dialect": "gulf",
    "dialect_confidence": 0.95,
    "requires_response": true,
    "priority": "medium",
    "suggested_reply": {
      "text": "سؤال عن موعد الجزء الثاني من المحتوى",
      "direction": "rtl"
    },
    "provider": "allam",
    "model": "sdaia/allam-1-13b-instruct",
    "fallback_used": false,
    "tokens_used": 142,
    "cost_usd": 0.006,
    "credits_used": 6
  },
  "errors": [],
  "request_id": "req_nw_a1b2c3d4e5f67890abcdef12"
}
```

### Response headers

| Header | Description |
|--------|-------------|
| `X-Request-Id` | Unique request identifier for debugging |
| `X-RateLimit-Limit` | Rate limit ceiling for current window |
| `X-RateLimit-Remaining` | Requests remaining in current window |
| `X-RateLimit-Reset` | Window reset time (RFC 3339) |
| `X-NAWA-Cache` | `HIT` if served from semantic cache (no cost) |
| `X-NAWA-Provider` | AI provider used: `claude`, `gemini`, or `allam` |
| `X-NAWA-Latency` | Processing time in milliseconds |
| `X-NAWA-Calibration-Version` | Calibration version applied (if any) |

### Result fields

| Field | Type | Description |
|-------|------|-------------|
| `id` | string | Classification ID (`cls_nw_xxx` format) |
| `object` | string | Always `classification` |
| `intent` | string[] | Detected intents: `question`, `complaint`, `praise`, `suggestion`, `spam`, `other` |
| `sentiment` | string | `positive`, `negative`, `neutral`, `mixed` |
| `language` | string | Detected language: `ar`, `en`, `mixed` |
| `dialect` | string or null | Arabic dialect: `gulf`, `egyptian`, `levantine`, `maghrebi`, `msa`. Null if not Arabic. |
| `dialect_confidence` | number or null | Confidence score 0-1 for dialect detection. Null if not Arabic. |
| `requires_response` | boolean | Whether the comment warrants a reply |
| `priority` | string | Suggested priority: `high`, `medium`, `low` |
| `suggested_reply` | object | Suggested reply with `text` and `direction` (`ltr` or `rtl`) |
| `provider` | string | AI provider used: `claude`, `gemini`, `allam` |
| `model` | string | Model version used |
| `fallback_used` | boolean | Whether a fallback provider was used |
| `tokens_used` | integer or null | Tokens consumed |
| `cost_usd` | number | Cost in USD |
| `credits_used` | number | Credits deducted |

### Error responses

| Status | Type | When |
|--------|------|------|
| 400 | `invalid_request_error` | Missing `text`, text too long |
| 401 | `authentication_error` | Invalid or missing API key |
| 402 | `insufficient_credits` | No credits remaining |
| 429 | `rate_limit_error` | Rate limit exceeded |
| 500 | `api_error` | Internal or provider error |

### More Arabic examples

<AccordionGroup>
  <Accordion title="Gulf Arabic - Praise">
    ```bash
    curl -X POST https://api.trynawa.com/v1/classify \
      -H "Authorization: Bearer nawa_test_sk_xxx" \
      -H "Content-Type: application/json" \
      -d '{"text": "ما شاء الله عليك، محتوى رهيب!"}'
    ```

    Response:
    ```json
    {
      "success": true,
      "result": {
        "id": "cls_nw_ghi789jkl012",
        "object": "classification",
        "intent": ["praise"],
        "sentiment": "positive",
        "language": "ar",
        "dialect": "gulf",
        "dialect_confidence": 0.93,
        "requires_response": false,
        "priority": "low",
        "suggested_reply": {
          "text": "تعليق إيجابي يعبر عن إعجاب المستخدم بالمحتوى",
          "direction": "rtl"
        },
        "provider": "allam",
        "model": "sdaia/allam-1-13b-instruct",
        "fallback_used": false,
        "tokens_used": 128,
        "cost_usd": 0.006,
        "credits_used": 6
      },
      "errors": [],
      "request_id": "req_nw_ghi789jkl012mnopqr34"
    }
    ```
  </Accordion>

  <Accordion title="Egyptian Arabic - Complaint">
    ```bash
    curl -X POST https://api.trynawa.com/v1/classify \
      -H "Authorization: Bearer nawa_test_sk_xxx" \
      -H "Content-Type: application/json" \
      -d '{"text": "الصوت وحش أوي في الفيديو ده"}'
    ```

    Response:
    ```json
    {
      "success": true,
      "result": {
        "id": "cls_nw_mno345pqr678",
        "object": "classification",
        "intent": ["complaint"],
        "sentiment": "negative",
        "language": "ar",
        "dialect": "egyptian",
        "dialect_confidence": 0.97,
        "requires_response": true,
        "priority": "high",
        "suggested_reply": {
          "text": "شكوى عن جودة الصوت في الفيديو",
          "direction": "rtl"
        },
        "provider": "allam",
        "model": "sdaia/allam-1-13b-instruct",
        "fallback_used": false,
        "tokens_used": 135,
        "cost_usd": 0.006,
        "credits_used": 6
      },
      "errors": [],
      "request_id": "req_nw_mno345pqr678stuvwx90"
    }
    ```
  </Accordion>

  <Accordion title="Levantine Arabic - Suggestion">
    ```bash
    curl -X POST https://api.trynawa.com/v1/classify \
      -H "Authorization: Bearer nawa_test_sk_xxx" \
      -H "Content-Type: application/json" \
      -d '{"text": "لو سمحت حاول تحكي عن المطاعم بلبنان"}'
    ```

    Response:
    ```json
    {
      "success": true,
      "result": {
        "id": "cls_nw_stu901vwx234",
        "object": "classification",
        "intent": ["suggestion"],
        "sentiment": "neutral",
        "language": "ar",
        "dialect": "levantine",
        "dialect_confidence": 0.96,
        "requires_response": true,
        "priority": "medium",
        "suggested_reply": {
          "text": "اقتراح لتغطية موضوع المطاعم في لبنان",
          "direction": "rtl"
        },
        "provider": "claude",
        "model": "claude-3-5-haiku-20241022",
        "fallback_used": false,
        "tokens_used": 140,
        "cost_usd": 0.006,
        "credits_used": 6
      },
      "errors": [],
      "request_id": "req_nw_stu901vwx234yzab5678"
    }
    ```
  </Accordion>

  <Accordion title="With provider override (A/B testing)">
    Force a specific provider using the `provider` query parameter:

    ```bash
    curl -X POST "https://api.trynawa.com/v1/classify?provider=claude" \
      -H "Authorization: Bearer nawa_test_sk_xxx" \
      -H "Content-Type: application/json" \
      -d '{"text": "ماشاء الله الفيديو حلو مرة"}'
    ```
  </Accordion>
</AccordionGroup>
