---
title: "Authentication"
sidebarTitle: "Authentication"
description: "API key management, free vs live modes, and security best practices."
---

All NAWA API requests require a Bearer token in the `Authorization` header.

```bash
Authorization: Bearer nawa_live_sk_xxx
```

## API key formats

| Environment | Prefix | Example |
|-------------|--------|---------|
| Free | `nawa_test_sk_` | `nawa_test_sk_a1b2c3d4e5f67890...` |
| Live | `nawa_live_sk_` | `nawa_live_sk_x9y8z7w6v5u43210...` |

Keys contain a 32-character hex suffix after the prefix (e.g., `nawa_live_sk_` + 32 hex characters).

## Free vs live keys

<CardGroup cols={2}>
  <Card title="Free" icon="flask">
    **Free, no credit card required.**
    - 100 lifetime requests (paid endpoints only)
    - 14-day key expiry
    - Uses real AI models (same accuracy)
    - Rate limit: 10 requests/minute
    - Free endpoints (health, usage, analytics, comments, feedback) do not count toward the 100 limit
    - Perfect for prototyping and testing
  </Card>
  <Card title="Live" icon="rocket">
    **Pay-as-you-go with credit packs.**
    - Unlimited requests (with credits)
    - No key expiry (rotate at 90 days recommended)
    - Full rate limits by tier (Growth, Enterprise, Enterprise+)
    - Webhook support
    - Analytics and usage dashboards
  </Card>
</CardGroup>

## Tier assignment

Your API key tier determines your rate limits:

| Tier | How you get it | Rate limit |
|------|---------------|------------|
| **Sandbox** | Free keys (`nawa_test_sk_`) | 10 req/min |
| **Trial** | Live keys with minimal credits | 60 req/min |
| **Growth** | Live keys with credits | 120 req/min |
| **Enterprise** | Contact [sales@trynawa.com](mailto:sales@trynawa.com) | 300 req/min |
| **Enterprise+** | Contact [sales@trynawa.com](mailto:sales@trynawa.com) | 1,000 req/min |

When you create a key with credits on your account, you receive a live key at the Growth tier. If you have no credits, you receive a free key at the Sandbox tier.

## Creating API keys

<Steps>
  <Step title="Sign in to your account">
    Go to [trynawa.com/developers/keys](https://trynawa.com/developers/keys) and sign in with your account.
  </Step>
  <Step title="Create a new key">
    Click **Create Key** and give it a name (max 100 characters).
  </Step>
  <Step title="Copy your key">
    Your key is displayed **once** at creation. Copy it immediately and store it securely.

    <Warning>
      API keys are shown only once at creation. They are stored as SHA-256 hashes -- we cannot retrieve your key later. If you lose it, revoke the old key and create a new one.
    </Warning>
  </Step>
</Steps>

## Key rotation

We recommend rotating live keys every **90 days**. To rotate without downtime:

1. Create a new key in the dashboard
2. Update your application to use the new key
3. Verify requests succeed with the new key
4. Revoke the old key

<Tip>
  You can have up to 5 active keys per environment. This allows zero-downtime rotation by running old and new keys simultaneously.
</Tip>

## Key revocation

Revoke a key instantly from the dashboard under **Settings > API Keys**. Revoked keys return a `401` error with code `key_revoked` immediately.

## Security best practices

- **Never commit keys to source control.** Use environment variables or a secret manager.
- **Use free keys for development.** Reserve live keys for deployed applications.
- **Monitor usage** in the dashboard to detect unauthorized use.
- **Rotate keys** every 90 days or immediately if compromised.

```typescript
// Load from environment
const nawa = new Nawa({ apiKey: process.env.NAWA_API_KEY })

// Never hardcode keys
const nawa = new Nawa({ apiKey: 'nawa_live_sk_abc123' })
```
