---
title: "Dashboard"
sidebarTitle: "Introduction"
description: "Arabic-first AI comment classification. Detect Gulf, Egyptian, Levantine, and MSA dialects with industry-leading accuracy."
---

<img
  className="block dark:hidden"
  src="/images/hero-light.png"
  alt="NAWA API"
  style={{ display: 'none' }}
/>

# The Arabic-first AI classification API

NAWA is purpose-built for Arabic social media. Classify comments by intent, sentiment, dialect, and priority -- powered by multi-provider AI routing with ALLaM, Claude, and Gemini.

<CardGroup cols={2}>
  <Card title="Classify comments" icon="tags" href="/api-reference/classify">
    Detect intent, sentiment, dialect, and priority in a single API call. Gulf, Egyptian, Levantine, Maghrebi, and MSA supported.
  </Card>
  <Card title="Detect dialects" icon="language" href="/guides/arabic-dialect-detection">
    Distinguish Gulf from Egyptian, Levantine, and MSA Arabic with high accuracy.
  </Card>
  <Card title="Generate replies" icon="reply" href="/api-reference/comments-reply">
    Generate contextual, dialect-matched replies to comments automatically.
  </Card>
  <Card title="Track usage" icon="chart-mixed" href="/api-reference/analytics">
    Monitor API usage, costs, cache performance, and provider distribution.
  </Card>
</CardGroup>

## Why NAWA?

| Metric | Value |
|--------|-------|
| Dialects supported | Gulf, Egyptian, Levantine, Maghrebi, MSA |
| Cost per classify | $0.006 (6 credits) |
| Free tier | 100 requests, no credit card |
| Providers | ALLaM, Claude, Gemini |
| Semantic caching | Cache hits are free |

## Quick taste

Make your first classification in seconds:

<CodeGroup>

```bash cURL
curl -X POST https://api.trynawa.com/v1/classify \
  -H "Authorization: Bearer nawa_test_sk_xxx" \
  -H "Content-Type: application/json" \
  -d '{"text": "متى الجزء الثاني؟"}'
```

```typescript TypeScript
const response = await fetch('https://api.trynawa.com/v1/classify', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer nawa_test_sk_xxx',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({ text: 'متى الجزء الثاني؟' })
})

const data = await response.json()
console.log(data.result.intent)   // ["question"]
console.log(data.result.dialect)  // "gulf"
```

```python Python
import requests

response = requests.post(
    "https://api.trynawa.com/v1/classify",
    headers={
        "Authorization": "Bearer nawa_test_sk_xxx",
        "Content-Type": "application/json"
    },
    json={"text": "متى الجزء الثاني؟"}
)

data = response.json()
print(data["result"]["intent"])   # ["question"]
print(data["result"]["dialect"])  # "gulf"
```

</CodeGroup>

<Card title="Get started in 3 minutes" icon="rocket" href="/quickstart">
  Get your API key and make your first call -- no credit card required.
</Card>
