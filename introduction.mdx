---
title: "Dashboard"
sidebarTitle: "Introduction"
description: "Arabic-first AI text classification. Detect Gulf, Egyptian, Levantine, and MSA dialects with industry-leading accuracy."
---

<img
  className="block dark:hidden"
  src="/images/hero-light.png"
  alt="NAWA API"
  style={{ display: 'none' }}
/>

# The Arabic-first AI classification API

NAWA is purpose-built for Arabic social media. Classify text by intent, sentiment, dialect, and priority -- powered by [HUMAIN's ALLaM](https://humain.com) large language model for Arabic.

<CardGroup cols={2}>
  <Card title="Classify text" icon="tags" href="/api-reference/classify">
    Detect intent, sentiment, dialect, and priority in a single API call. Gulf, Egyptian, Levantine, Maghrebi, and MSA supported.
  </Card>
  <Card title="Detect dialects" icon="language" href="/guides/arabic-dialect-detection">
    Distinguish Gulf from Egyptian, Levantine, and MSA Arabic with high accuracy using ALLaM.
  </Card>
  <Card title="Generate replies" icon="reply" href="/api-reference/comments-reply">
    Classify a comment and generate a culturally-aware, dialect-matched reply in one call.
  </Card>
  <Card title="Custom rubrics" icon="list-check" href="/api-reference/rubric-classify">
    Define your own categories and classify text against a custom rubric.
  </Card>
</CardGroup>

## Why NAWA?

| Metric | Value |
|--------|-------|
| Dialect support | Gulf, Egyptian, Levantine, Maghrebi, MSA |
| p95 latency | &lt;2 seconds |
| Cost per classify | $0.006 |
| Free tier | 100 requests, no credit card |
| Uptime SLA | 99.9% |

## Quick taste

Make your first classification in seconds:

<CodeGroup>

```bash cURL
curl -X POST https://api.trynawa.com/v1/classify \
  -H "Authorization: Bearer nawa_test_sk_xxx" \
  -H "Content-Type: application/json" \
  -d '{"text": "متى الجزء الثاني؟"}'
```

```typescript TypeScript
import { Nawa } from '@nawalabs/sdk'

const nawa = new Nawa({ apiKey: 'nawa_test_sk_xxx' })
const { data } = await nawa.classify({
  text: 'متى الجزء الثاني؟'
})
console.log(data.intent) // ["question"]
```

```python Python
from nawa import Nawa

nawa = Nawa(api_key="nawa_test_sk_xxx")
result = nawa.classify(
    text="متى الجزء الثاني؟"
)
print(result.data.intent)  # ["question"]
```

</CodeGroup>

<Card title="Get started in 3 minutes" icon="rocket" href="/quickstart">
  Get your API key and make your first call -- no credit card required.
</Card>
